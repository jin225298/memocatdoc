# memoCat · 设计文档

> 面向读者:评审与使用者。内容以系统能力、设计思路与关键技术方案为主。

---

## 1 · 项目概述

### 1.1 背景与问题

人产生信息最密集的时刻是口头表达:讨论、授课、问诊、头脑风暴——最有价值的内容几乎都是在说话中产生的,也流失得最彻底。录音与转写早已商品化,但缺的是最后一步:**把说过的话,变成下次真正能用的知识**。现有工具的普遍问题是"录了、转了、整了,最后还是不看"——记录的价值由**被利用的概率**决定,而非采集的便利性。

### 1.2 产品定位

**memoCat** 是一个本地优先、常驻后台的桌面语音 Agent。透明桌宠是唯一入口,一套麦克风音频统一承接五种能力:

| 能力 | 说明 |
|---|---|
| 环境记录 | 后台持续监听 → 语音分段 → 转写 → 说话人识别 → 场景判断 → 自动整理入库 |
| 语音输入法 | 按需录音,转写后直接插入**当前前台应用的输入光标** |
| 实时语音对话 | 与云端大模型全双工交流,随时打断;复杂事务委派后台 Agent 执行 |
| 记忆检索 | 对全部原始对话与整理报告提问,回答必带出处 |
| 提醒待办 | 从对话自动提取行动项,到期提醒,可确认可修改 |

---

## 2 · 设计思路

### 2.1 总体思路

```
用户按一次开始、按一次结束
  → 自动完成:监听 / 分段 / 转写 / 说话人分离 / 场景判断 / 结构化整理 / 溯源入库
  → 平时无需打开任何界面,需要时随时找得到、问得到
```

### 2.2 核心设计原则

| 原则 | 为什么 | 落地 |
|---|---|---|
| 自动化彻底 | 记录与思考争夺同一份注意力,任何需要用户介入的环节都会让产品退化 | 全链路自动流水线,用户只控制录音开关 |
| 整理规则场景化 | 数学课要公式、组会要进展、问诊要病历,统一模板是"面面俱到却毫无重点" | 整理被建模为**可声明、可版本化的整理插件(Package)**,用户按需定义 |
| 新增事实 = 0 · 必可溯源 | 报告与回答的可信度由原始记录担保 | 每条结论携带 SourceSpan,可跳回原始语音;检索不到就说"没找到" |
| 本地优先 | 语音数据是隐私资产,可用性与可靠性不能押在云端 | 音频/声纹/对话/报告/Agent 状态全在本机,上云仅必要片段 |

### 2.3 关键取舍(为什么这么做,而不是别的)

| 决策点 | 选择 | 理由 |
|---|---|---|
| 整理方式 | 声明式 Package + 编译器 + 运行时 Guard | 规则可验证、可版本化、可回放,而不是黑盒 Prompt |
| 检索技术 | SQLite FTS5 + 时间/说话人/场景过滤 | 中文口语查询在强元数据过滤下召回已足够;向量检索留作升级路径 |
| 回声处理 | 播放引用对齐(参考信号),非自适应滤波 | 全双工下"知道自己放了什么",算法更稳、无收敛风险 |
| 语音对话 | 前台实时问答 + 后台 Agent 委派 | 低延迟交互与深任务执行互不阻塞,各自可替换 |

---

## 3 · 系统总体架构

### 3.1 技术路线

| 层 | 选型 |
|---|---|
| 桌面壳 | Tauri 2(透明无边框桌宠窗口,原生 IPC 二进制通道) |
| 后端 | Rust workspace(25 crate),tokio 异步,`cpal` 采集、`ort` 本地推理、`rusqlite` 存储 |
| 前端 | React 19 + TypeScript + Vite,多窗口 |
| 本地模型 | CAM++ 声纹(ONNX,256 维)、WebRTC VAD |
| 云端服务 | Qwen Filetrans / MiMo ASR、Qwen Realtime WebSocket、DeepSeek LLM、MemOS 长期记忆(可选) |
| 本地 Agent | Hermes(ACP 子进程)+ 双 MCP sidecar(记忆访问 / 会话路由) |

### 3.2 分层架构
![](10克技术报告/技术架构.png)
### 3.3 核心子系统

| 子系统 | 职责 | 关键事实 |
|---|---|---|
| AudioHub | 唯一物理麦克风 owner,把同一路音频分发给多个能力 | Ambient / Dialogue / Dictation 三类消费者独立启停,互不抢占 |
| 采集管线 | VAD 分段 → 转写 → 声纹 → 场景 → 整理,全自动 | 静音 1.2 秒封块、12 秒硬切,作业持久化、重启续跑、幂等去重 |
| Agent 运行时 | 前台实时对话 + 后台复杂任务委派 | 前台 Qwen Realtime(低延迟),后台 Hermes(深任务),crate 级隔离 |
| 记忆库 | 双路检索(原始记录 / 整理报告)+ 可选远端记忆同步 | FTS5 trigram 全文索引,带来源的答案合成 |

### 3.4 数据与存储设计

| 库 | 用途 |
|---|---|
| voiceagent / dialogue.db | 主库、对话时间线、音频段、整理报告 |
| vault.sqlite3 | 记忆库:FTS5 索引 + 反链 + 同步 Outbox + 审计 |
| agent / scheduler / approvals | 会话与执行模式、提醒调度、审批 |
| voiceprints.sqlite3 | 声纹库(临时 / 正式两档) |

- 原始音频按 **SHA-256 内容寻址**落盘,数据库只存相对键与不可变元数据,不存用户绝对路径
- 多条记录可回放:对话句子 → 音频切片 → 语音,逐级可跳转

### 3.5 配置与密钥管理

密钥采用分层解析(环境变量 → 应用设置 → 本地密钥目录 → 迁移自旧配置),**只用于构造请求头**,不进入界面、日志与运行记录;密钥变更自动轮换凭证。

---

## 4 · 功能模块设计

### 4.1 环境记录流水线

| 环节 | 机制 | 关键参数 |
|---|---|---|
| 分段 | WebRTC VAD + 语音块累积 | 30 ms 帧;静默 1.2 s 封块;连续 12 s 硬切;<500 ms 碎片丢弃 |
| 转写 | 文件级转写 + 作业持久化 | 异步任务轮询;句 ID 确定性派生,重放不重复 |
| 落库 | 逐句声纹确认后事务写入 | 最终身份以本地声纹为准,云端标签仅作辅助 |
| 双路径 | 环境记录 / 录音听写各自独立管线 | 60 s 分块、每块终身重试预算、`budget_exceeded` 终态 |

### 4.2 说话人识别(本地 ONNX)

- CAM++ 声纹模型:16 kHz → 80 维 Fbank → **256 维向量** → 余弦相似度 1:N 匹配
- **双阈值**:临时声纹 0.85 / 正式声纹 0.90;少于 3 秒不建库,足够的累积语音自动升级入正式库
- 一次对话中未识别的新声音自动建立临时声纹,对话结束丢弃——不主动制造"陌生人身份"
- 工作台可手动登记、改名、合并、删除,正式声纹全链路生效

### 4.3 场景判断:语义判定 + 时间确认状态机

场景边界**只由大模型语义判定**,再用时间窗口吸收不确定性:

| 机制 | 设计 |
|---|---|
| 语义判定 | 所有整理插件的判定规则拼进同一次调用,输出结构化判定(start / end / stay / none) |
| 时间确认 | 开始需观察 **30 秒**话题持续;结束需观察 **3 分钟**话题未迁回;确认事件**回溯到首个提示记录**,边界不丢内容 |
| 抗幻觉 | 重复判定降级、无效引用无害化、"话题迁回"取消结束候选 |
| 触发频率 | 每 10 条记录或 45 秒判定一次,控制模型开销 |

### 4.4 整理引擎与整理插件(Package)

- **声明式 Package**:整理规则 = 清单 + 工作流 + 输入/输出 Schema + 提示词 + 渲染模板,内置课堂笔记、会议纪要、问诊记录、产品讨论等
- **编译与校验**:Package 经编译器静态检查(30 类诊断码、DAG 无环),产出带**哈希标识**的执行计划——同一版本行为可复现
- **运行 Guard**:模型调用全局预算、输出 JSON Schema 与溯源双重校验,**校验失败自动定向修复**(修复 = 1 次模型调用,受预算约束)
- **溯源硬约束**:每条结论必须能映射回原始句子的 `segmentId + quote`,引用不实即视为违规

### 4.5 记忆与检索

- **索引即事实源**:整篇原文进 FTS5(trigram + bm25),本地不存向量摘要,回读不二次读盘
- **双路召回、四象限合并**:整理报告命中 → 直接可用结论;原始记录命中 → 原话证据;报告遗漏 → 记为**自进化强反馈信号**;双空 → 确定性"没找到"
- **中文口语检索专项**:整句查询零命中时逐级放松(严格短语 → trigram → 双字元组计分召回),解决"该想起却想不起来"
- **远端记忆(可选)**:Outbox 本地优先异步同步;召回带 600 ms 预算,超时静默退回本地;候选必须能回落到本地仍有效的证据位置("不伪造出处")
- 收敛口径:普通提问 0.70 阈值;"上次/昨天聊过什么"类回忆句式自动放宽到 0.45,避免"有记忆却召不回"

### 4.6 实时语音对话(前台 Agent)

- **协议在本地,浏览器只播音频**:WebSocket 16 kHz 上行 / 24 kHz 下行,凭证不出本地进程;前端只消费类型化事件并连续播放 PCM
- 6 个函数工具:记忆召回 / 记忆写入 / 提醒 / 待办列表 / 待办更新 / **后台任务委派**
- 随时打断:重新开口即原子清空排队播报、隔离迟到音频;打断不影响环境记录,也不取消已提交的后台任务
- 会话持久化:关窗重开恢复最近会话,显式"新对话"才新建;断线重连幂等

### 4.7 后台任务委派(Hermes)

- Hermes 是本地子进程(ACP 协议),通过 MCP 访问记忆库工具与受限会话工具,**写操作一律经审批**,桌宠气泡一键批准/拒绝
- **三路路由**:普通问答、明确新任务、明确续接旧任务,deny-by-default;防误续接采用持久化授权集合,不受提示词左右
- **崩溃一致**:委派前二阶段路由登记(CAS),崩溃后由周期 tick 从持久化状态恢复,重放幂等
- **终态回注**:子任务结果只经原会话回注,窗口只展示权威终稿;语音播报与文字终稿不互相覆盖
- 安全:会话作用域 + 每次启动生成的 capability token 双重校验,拒绝跨会话 / 跨库调用

### 4.8 提醒与待办

- 明确语音指令直接建任务;对话中推断的事项先进候选,一行确认后生效(去重指纹 + 忽略抑制)
- 无截止时间的明确归属事项静默入收件箱;归属不明 / 来源缺失不自动调度
- 投递**至多一次**(唯一键 + 条件状态更新);漏投只补最近一次
- 完成只认明确完成 / 取消 / 关联任务确实成功;弱信号不改变任务状态

### 4.9 语音输入法(跨应用文字插入)

- 录音 → 后台转写 → **插入到转写完成时的前台应用光标处**(而非开始录音时)
- 三级降级:系统辅助功能插入 → 剪贴板 + 模拟粘贴 → 仅剪贴板;并如实告知用户用了哪种方式
- 剪贴板保护:插入前快照,内容未被改动才还原;目标在桌宠自身内时只写剪贴板

### 4.10 桌宠交互

- 冷启动只显示透明桌宠,悬停出现工具条(记录 / 对话 / 听写 / 设置 / 退出);退出即真实退出并释放麦克风
- 气泡只承载必要事件:任务确认、审批、到期提醒、终态结果与录音异常;常驻行为零打扰
- 桌面与应用内共用同一套宠物组件与状态动画,"正在听 / 思考 / 回答 / 工作"一目了然

### 4.11 插件创作与自进化

- 用户用自然语言描述整理需求 → 自动生成整理插件 → **在历史对话上回放试跑** → 满意后发布
- 已发布版本不可变,修改产生新版本,可随时回退
- 自进化只产出**改进提案**(反馈来源:报告被编辑、字段被标记无用、边界被修正、检索提问暴露缺失),必须经回放对比 + 用户批准才生效

### 4.12 前端工作台

- 工作台:实时采集、任务中心、内容库、提醒日历;设计台:插件列表、可视化工作流设计器、运行记录、数据谱系
- **可视化工作流编辑器**:拖拽节点、连线、拒绝成环;保存前静态检查(10 类问题,0 问题才可保存)
- 报告渲染:结构化字段 + Markdown / 公式(KaTeX)渲染;时间线 / 报告 / 原始录音多级联动

---

## 5 · 关键技术难点与解决方案

| # | 难点 | 方案 |
|---|---|---|
| 1 | 一套麦克风同时服务多种能力,不能互相抢占 | 单一音频枢纽 + 多消费者隔离:环境记录为主线,其余按需订阅、独立启停;故障留痕而非静默丢帧 |
| 2 | 全双工语音对话中,助手自己的声音回流形成回声 | 播放引用登记 + 互相关对齐判"助手下行",外层再加时序门,双层防护 |
| 3 | 长时运行的崩溃一致性 | 转录 / 转写 / 委派 / 回注全链路持久化作业 + 确定性 ID + 二阶段登记,重启即恢复,重放不重复 |
| 4 | 大模型判定的不确定性与幻觉 | 时间确认窗口吸收抖动;重复判定降级、无效引用无害化;判定与整理均设模型预算上限 |
| 5 | 中文口语整句几乎无法被 FTS 检索到 | 三档逐级放松的召回链,以命中计分排序兜底,召回与相关性兼得 |
| 6 | 低延迟对话与长时间复杂任务互相阻塞 | 前后台双 Agent 架构,crate 级隔离,仅有事件 / 命令 / 回注几个接缝,互不阻塞、各自可替换 |
| 7 | 本地优先与可用性的平衡 | 每个模型环节均定义"无密钥/失败时的行为"(降级、确定性答案或显式报错),降级有边界 |
| 8 | 桌面级深度集成 | 透明桌宠、真实麦克风、跨应用文字插入、媒体键、系统权限,全部走系统原生能力,非浏览器模拟 |

---

## 6 · 系统特色与创新点

| 维度 | 同类产品(飞书妙记 / 讯飞听见 / Otter.ai 等) | memoCat |
|---|---|---|
| 整理规则 | 统一预设模板,人人一样 | **按场景可定制、可版本化、可自进化** |
| 使用方式 | 打开应用、新建记录 | **常驻无感的桌宠入口**,说完即得 |
| 答案可信度 | 摘要无出处 | **每条结论可跳回原始语音**,缺失即明说 |
| 数据归属 | 云端为主 | **本地优先**,云端仅作可选旁路 |
| 能力边界 | 转写 + 摘要 | 录音整理 + 语音输入 + 实时对话 + 记忆 + 待办,同一入口 |

---

## 7 · 总结与展望

memoCat 把"语音记录 → 自动整理 → 随时可用"做成一条完整闭环:采集与整理彻底自动化,整理方式由用户定义并持续进化,一切结论可溯源、一切数据本地优先——让使用者在"认真听"和"认真记"之间不再二选一。

**展望**:

- 在真实生活场景中持续调优场景判断、声纹与检索准确率
- 插件创作与自进化生态从后端能力走向完整可视化产品
- 扩展整理算子的表达能力(多节点工作流)
- 记忆与对话更深层打通(时间、说话人、场景进入检索条件)
- 多端形态(移动端)与更丰富的桌宠形态