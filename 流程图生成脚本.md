# memoCat · 流程图生成脚本

> 本文件收录设计文档中所有流程图的 GPT-Image 生成提示词。生成结果请保存到 `images/`。

## 统一风格约束

```text
所有图片使用同一视觉系统：白色或浅灰背景、深蓝主色、青绿色强调色、橙色表示审批或 Agent 事件；扁平化技术架构图；圆角矩形节点；方向明确的箭头；中文标签清晰可读；无渐变、无阴影堆叠、无 3D、无人物、无品牌 Logo、无装饰性图标；画布比例 16:9；适合嵌入中文 Markdown 设计文档。
```

## O-01 · 产品总体结构示意图

```text
请生成一张中文软件系统架构示意图，标题为“memoCat 产品总体结构”。画布比例 16:9，白色背景，扁平化、专业、简洁，适合放入设计文档。

图中从上到下表现三个区域：
1. 顶部“用户输入”，包含三个并列的小节点：“环境语音记录”“实时语音对话”“文本输入”。
2. 第二层“信息系统”，在同一个大容器内分为“实时处理”和“数据与记忆”两个内部区域。实时处理区域包含“音频采集”“实时转写”“说话人识别”“场景判断”“个性化整理”；数据与记忆区域包含“原始音频与转写”“结构化报告”“Memory System”“待办与提醒”。
3. 底部“交互与执行系统”，包含“桌宠”“Dashboard”“实时语音 Agent”“Coding Agent”。

用粗实线箭头表示主数据流：用户输入 → 信息系统 → 交互与执行系统。用细线表示信息系统内部从实时处理区域到数据与记忆区域的数据流，以及“Memory System”向“实时语音 Agent”和“Coding Agent”提供上下文；用虚线表示“个性化整理”与“整理器管理”的配置关系。所有中文必须清晰、准确，不要出现英文乱码，不要添加图中未指定的模块。
```

## A-01 · 系统总体数据流图

```text
请生成一张中文技术架构流程图，标题为“memoCat 系统总体数据流”。画布比例 16:9，白色背景，深蓝色主流程，青绿色表示结构化数据，橙色表示 Agent 事件，采用左到右布局。

左侧放置“连续音频”节点；箭头连接到“VAD 与分段”；随后连接到“实时 ASR”和“声纹识别”两个并行节点；两个节点汇合到“统一时间线”。从“统一时间线”向右分出三条流：
- 上方：进入“场景判断”，再进入“智能整理”，输出“结构化报告”；
- 中间：进入“原始记录与证据索引”；
- 下方：进入“Action Item 提取”，输出“待办与提醒”。

“结构化报告”“原始记录与证据索引”“待办与提醒”汇合到“Memory System”。“Memory System”再连接到右侧的“实时对话 Agent”和“Coding Agent”。

在图底部增加一条细虚线说明：“音频、文本、结构化数据和 Agent 事件通过稳定 ID、时间戳和 SourceSpan 建立关联”。要求所有箭头方向明确，节点文字简短，禁止加入装饰插画、人物、品牌 Logo 或额外说明。
```

## R-01 · 信息系统分层与信息流图

```text
请生成一张中文分层技术架构图，标题为“memoCat 信息系统架构”。画布比例 16:9，采用从左到右的五层布局，每层使用浅色圆角容器，层与层之间用方向箭头连接。

第一层标题“音频输入层”，节点为“麦克风音频流”“统一音频枢纽”。
第二层标题“实时理解层”，节点为“VAD 分段”“实时语音转写”“本地声纹识别”。其中 VAD 分别连接 ASR 和声纹识别。
第三层标题“统一时间线”，节点为“转写片段”“时间戳”“说话人标签”“原始音频引用”。前三者汇合到一个“可追溯片段”节点。
第四层标题“语义编排层”，节点为“场景判断状态机”“个性化整理器”“增量整理”“最终校验”。
第五层标题“数据与记忆层”，节点为“原始记录”“结构化报告”“SourceSpan”“Memory System”。

用蓝色实线表示音频流，用深绿色实线表示文本流，用橙色虚线表示状态和配置流。图中明确标注：场景判断只选择整理器，整理器执行结构化输出，Memory System 同时接收原始记录和报告。不要描绘用户操作，不要加入桌宠或 Dashboard，不要使用复杂 3D 风格。
```

## I-01 · 交互系统与 Agent 调用流程图

```text
请生成一张中文交互系统流程图，标题为“memoCat 交互系统架构”。画布比例 16:9，白色背景，采用中心辐射与后台并行相结合的布局，风格与其他 memoCat 架构图一致。

左侧放置“用户语音 / 文本”节点，连接到中央的“桌宠入口”。从“桌宠入口”连接到“实时语音对话”。“实时语音对话”向右分出五个工具分支：
1. “Memory 召回”；
2. “报告与原文检索”；
3. “待办与提醒”；
4. “个性化整理器管理”；
5. “Coding Agent 委派”。

“个性化整理器管理”继续连接到“整理器草稿 → 回放测试 → 版本发布”。“Coding Agent 委派”连接到下方独立的后台区域，区域内依次显示“任务创建”“上下文授权”“后台执行”“事件流”“审批 / 取消”“结果回注”。

所有工具分支的结果汇合到右侧“结构化结果 / 语音回复 / 状态通知”。从“审批 / 取消”向“桌宠入口”画回流箭头。用实线表示命令流，用虚线表示事件流，用橙色表示需要用户审批的节点。不要把 Coding Agent 画成实时对话的一部分，要明确表现它在后台并行执行。
```

## 文件命名

```text
O-01 → images/architecture-overview.png
A-01 → images/system-data-flow.png
R-01 → images/recording-system-architecture.png
I-01 → images/interaction-agent-flow.png
```

## 交付检查

```text
1. 检查所有中文标签是否准确可读。
2. 检查箭头是否有明确方向，是否存在交叉后无法判断的连线。
3. 检查四张图的颜色、字体、节点形状和画布比例是否一致。
4. 检查每张图是否只表达一个主题，不要把交互细节塞进信息系统图。
5. 将图片放入 memocatdoc/images/，并在设计文档中替换对应占位符。
```

